const initState = {
    cities: [
        {id: '1', name: 'Austin'},
        {id: '2', name: 'Tokyo'},
    ]
};
const cityReducer = (state = initState, action) => {
    return state;
}

export default cityReducer;